# E.V. — Python dependencies
# Install:  pip install -r requirements.txt
#           playwright install chromium

# Web server + WebSocket
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
httpx>=0.27.0

# Local speech-to-text (offline). Pulls in PyAV for audio decoding.
faster-whisper>=1.0.0

# Optional Claude brain (only needed if llm_provider = "anthropic")
anthropic>=0.39.0

# Browser control + screen capture
playwright>=1.40.0
Pillow>=10.0.0

# System stats for the dashboard (/stats)
psutil>=5.9.0
